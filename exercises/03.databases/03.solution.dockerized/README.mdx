# Dockerized

PostgreSQL with TestContainers.

- Spawn a Testcontainer, point the tested app to its URL.

## Todos

- [ ] No way to run logic before `webServer`. Find another way to forward the container URL to the spawned app.
