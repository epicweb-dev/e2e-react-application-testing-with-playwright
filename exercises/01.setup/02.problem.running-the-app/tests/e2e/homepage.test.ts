import { test, expect } from '@playwright/test'

// 🐨 Create a new test called "displays the welcome heading".
// 💰 test('test title', async (context) => {})

// 🐨 In the test function arguments, destructure the test `context`
// and get the `page` object. You will use this object to interact
// with the browser page.
// 💰 async ({ page }) => {}

// 🐨 In the test function, navigate to the homepage of your app
// by calling `page.goto()` and providing it with the correct URL.
// 💰 await page.goto(url)

// 🐨 Finally, write an assertion that the heading with the text
// "The Epic Stack" is visible on the page.
// 💰 await expect(locator).toBeVisible()
// 💰 page.getByRole(role, { name })
