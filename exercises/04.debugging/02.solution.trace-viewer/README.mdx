# Trace viewer

- Show how to generate a trace file (both locally and on CI), which CLI commands to use.
- Show how to view a trace file from eslewhere (e.g. from a CI run).
- Use the trace file to track down the bug and fix it.
